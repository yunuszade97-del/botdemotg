# Telegram-бот для лидогенерации с LLM

MVP-каркас бота, который консультирует клиента, квалифицирует его в диалоге
и через function calling отдаёт готовый лид владельцу бизнеса в Telegram.
Заточен под услуги и аренду: авто, недвижимость, туры, трансферы.

Стек: Python 3.11+ · aiogram 3 · FastAPI · OpenAI-совместимый LLM API ·
SQLite (aiosqlite) · pydantic-settings.

---

## Быстрый старт (5 минут)

```bash
git clone <repo> && cd botdemotg
python3.11 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

cp .env.example .env && chmod 600 .env
# заполните BOT_TOKEN, ADMIN_CHAT_IDS, LLM_API_KEY

python -m app.preflight   # можно ли запускать?
python -m app.main
```

> **Первый запуск —** [`docs/LAUNCH.md`](docs/LAUNCH.md): пошаговый чек-лист
> до первого пришедшего лида, с проверкой на каждом шаге и сценарием показа
> клиенту.

`preflight` стоит запускать перед каждым деплоем. Он проверяет конфиг, запись
в БД, профиль ниши, токен, **достижимость админ-чатов** и **поддержку моделью
tool calling**, и возвращает код 1, если запускать нельзя:

```
[  OK  ] Конфигурация          режим polling, админов: 1
[  OK  ] База данных           data/bot.sqlite3
[  OK  ] Профиль ниши          rent_car: Batumi Rent — аренда автомобилей
[  OK  ] База знаний           content/niches/rent_car.md, 1753 символов
[  OK  ] Telegram: токен       @your_bot (id=123456789)
[ FAIL ] Telegram: админ-чаты  недоступны: [777]. Откройте бота с этих
                               аккаунтов и нажмите /start
[  OK  ] LLM: доступ           gpt-4o-mini отвечает
[  OK  ] LLM: tool calling     инструмент вызван (имя: Иван)
```

Бот поднимется в режиме long polling, HTTP-сервер — на `http://localhost:8080`
(`/healthz` для проверок живости).

Что нужно получить заранее:

| Значение          | Где взять                                                       |
|-------------------|-----------------------------------------------------------------|
| `BOT_TOKEN`       | [@BotFather](https://t.me/BotFather) → `/newbot`                 |
| `ADMIN_CHAT_IDS`  | [@userinfobot](https://t.me/userinfobot) → ваш `id`              |
| `LLM_API_KEY`     | [OpenAI](https://platform.openai.com/api-keys) или [OpenRouter](https://openrouter.ai/keys) |

> Важно: перед первым лидом напишите боту `/start` со своего админ-аккаунта.
> Telegram не даёт боту писать первым тому, кто с ним не общался.

---

## Ниши и переключение на нового клиента

Ниша — это данные, а не код. Одна переменная в `.env` меняет название,
направление, приветствие, прайс и **набор вопросов для квалификации**:

```dotenv
PROFILE=tours      # rent_car | real_estate | tours | ваша
```

```bash
python -m app.profiles              # список готовых ниш
python -m app.profiles show tours   # карточка + сценарий для демо
python -m app.profiles prompt tours # системный промпт как его увидит модель
```

Профиль — два файла: `profiles/<имя>.toml` (кто вы и что спрашивать) и
`content/niches/<имя>.md` (прайс и FAQ, его правит менеджер клиента).
Ключ `qualify` вклеивается в промпт: поэтому прокату бот спрашивает стаж
вождения, а экскурсиям — возраст детей.

Один профиль обслуживает много клиентов в одной нише — любая `COMPANY_*`
в `.env` перебивает профиль точечно:

```dotenv
PROFILE=tours
COMPANY_NAME=Ольга Travel
COMPANY_CITY=Тбилиси
```

Как завести свою нишу — [`docs/NICHES.md`](docs/NICHES.md).

Для демонстрации клиенту один бот умеет предлагать сразу несколько ниш на
выбор кнопками (`SHOWCASE_PROFILES=rent_car,real_estate,tours`) — режим
витрины описан там же, в [`docs/NICHES.md`](docs/NICHES.md).

Тон и логика продаж живут в `app/core/prompts.py` и общие для всех ниш —
правьте, только если меняете стиль общения, а не факты о бизнесе.

---

## Как это работает

```
Telegram
   │  (polling | webhook)
   ▼
FastAPI ──► aiogram Dispatcher ──► middlewares (логи, троттлинг)
                                        │
                                        ▼
                              ConversationService
                       ┌────────────────┼────────────────┐
                       ▼                ▼                ▼
                  история из      LLM + tools        save_qualified_lead
                    SQLite      (2 прохода)                 │
                                                   ┌────────┴────────┐
                                                   ▼                 ▼
                                              лид в SQLite    уведомление
                                                                 админу
```

**Флоу диалога.** Каждое сообщение уходит в LLM вместе с системным промптом и
окном последних `HISTORY_LIMIT` реплик. Модель отвечает как менеджер по
продажам: коротко, по одному вопросу за раз, и собирает недостающие поля заявки.

**Квалификация.** Модели доступны два инструмента.

`request_phone_button` — показать кнопку «Отправить мой номер». Модель сама
решает, когда в разговоре пора брать контакт. Номер приходит подтверждённым
самим Telegram: это надёжнее, чем номер, который модель вычитала из текста.

`save_qualified_lead` — сохранить заявку. Вызывается, как только известны имя,
контакт, сроки и суть запроса:

| Поле               | Тип             | Описание                                  |
|--------------------|-----------------|-------------------------------------------|
| `client_name`      | `str`           | имя клиента                               |
| `phone_or_contact` | `str`           | телефон, @username, t.me-ссылка или e-mail |
| `dates_or_timing`  | `str`           | когда нужна услуга                        |
| `service_details`  | `str`           | что именно нужно + пожелания              |
| `budget`           | `str \| None`   | бюджет, если обсуждался                   |
| `summary`          | `str`           | выжимка диалога в 1–2 предложения         |

**Уведомление.** Лид пишется в SQLite, затем уходит в чаты из `ADMIN_CHAT_IDS`:

```
🔥 НОВЫЙ ГОРЯЧИЙ ЛИД!

👤 Имя: Иван
📞 Контакт: +7 999 123-45-67
📅 Сроки/Даты: 12–19 августа
🚗 Запрос: Toyota RAV4, нужен автомат
💰 Бюджет: до 100 GEL в сутки
📝 Суть: Клиент ищет кроссовер на неделю в августе.
💬 Профиль: @ivan · открыть чат

Заявка #7 · 2026-09-03 12:00:00
```

Клиенту модель отвечает подтверждением («данные зафиксировал, менеджер
свяжется в течение 5 минут»).

---

## Решения, которые стоит знать до правок

Это не абстрактные best practices — каждый пункт закрывает конкретный способ
уронить продакшен или потерять деньги.

**Два прохода к LLM на один вызов инструмента.** После `save_qualified_lead`
результат возвращается модели tool-сообщением, и она генерирует подтверждение.
Один проход оставил бы клиента без ответа. Лимит — `LLM_MAX_TOOL_ROUNDS`;
на исчерпании отдаётся шаблонное подтверждение, а не тишина.

**Раунды с инструментами не попадают в историю.** В БД пишутся только
текстовые ходы. Сохранённое `assistant`-сообщение с `tool_calls` без парного
`tool`-ответа роняет следующий запрос к API в 400.

**Дедупликация лидов.** Модель охотно перевызывает инструмент на каждом
следующем сообщении. Повтор с тем же контактом в пределах
`LEAD_DEDUP_WINDOW_MINUTES` не создаёт новую заявку и не будит админа второй раз.

**Валидация аргументов инструмента.** Аргументы приходят из генерации модели —
это такой же недоверенный ввод, как и пользовательский. `phone_or_contact`
проверяется на реальный контакт; заглушки вроде «не указан» отклоняются, и
модель получает ошибку с указанием переспросить клиента.

**Кнопка «Отправить мой номер».** Номер из Telegram подтверждён платформой —
надёжнее, чем то, что модель «увидела» в тексте.

**Подряд идущие сообщения склеиваются в один ход.** Люди пишут очередями:
«Иван», «+7 999 123-45-67», «на август» — тремя сообщениями за пару секунд.
Бот ждёт `MESSAGE_AGGREGATION_DELAY` секунд тишины и отправляет очередь
в модель одним ходом. Без этого троттлинг отбрасывал бы всё, кроме первого
сообщения, и телефон, присланный вторым, просто терялся бы — ровно тот
провал, ради предотвращения которого бот и существует. Побочный эффект
приятный: три сообщения стоят одного вызова LLM вместо трёх.

**Последовательная обработка одного чата.** `asyncio.Lock` на `chat_id`:
три сообщения подряд не превращаются в три параллельных прохода и кашу в истории.

**Троттлинг включён по умолчанию, но не отбрасывает очередь.**
`THROTTLE_MIN_INTERVAL=0`: частые сообщения склеиваются, а не теряются.
От реального флуда защищает лимит сообщений в минуту, а от расходов —
склейка и `DAILY_LLM_CALLS_PER_USER`.

**Секрет вебхука проверяется всегда.** Заголовок `X-Telegram-Bot-Api-Secret-Token`
сверяется через `hmac.compare_digest` до любой другой работы и не зависит от
того, успел ли отработать `lifespan`. Без этого эндпойнт принимал бы поддельные
апдейты от кого угодно.

**Апдейты обрабатываются в фоне.** Вебхук отвечает `200` сразу: ответ LLM
занимает секунды, а Telegram на медленный ответ начинает слать ретраи того же
апдейта.

**Лид сохраняется до уведомления.** Упавшая отправка админу не теряет заявку —
она остаётся с `admin_notified = 0` и досылается при следующем старте.

**Деградация вместо тишины.** Если LLM недоступна после всех повторов, клиент
получает вежливое сообщение, а его реплика всё равно сохраняется в истории.
Тот же принцип у обработчика ошибок: любое необработанное исключение всё равно
заканчивается ответом клиенту. Молчащий бот — это ушедший клиент.

**Суточный лимит расходов.** Троттлинг ограничивает частоту, но не объём:
15 сообщений в минуту — это 21 600 платных вызовов в сутки с одного аккаунта.
`DAILY_LLM_CALLS_PER_USER` и `DAILY_LLM_CALLS_GLOBAL` ставят потолок; при его
достижении бот не замолкает, а предлагает оставить контакт кнопкой.

**Заявка из кнопки не зависит от LLM.** Если лимит исчерпан или модель лежит,
присланный кнопкой номер сохраняется напрямую, вместе с выжимкой из предыдущих
вопросов клиента. Человек, уже нажавший кнопку, — самый горячий лид из всех;
терять его из-за недоступности модели недопустимо.

**Только личные чаты.** Бота достаточно добавить в любую группу, чтобы он
отвечал на каждое сообщение платным вызовом LLM — один активный чат сжигает
дневной бюджет за час. `ALLOW_GROUP_CHATS=True` снимает ограничение осознанно;
чаты из `ADMIN_CHAT_IDS` пропускаются всегда.

**Владелец узнаёт о поломке от бота, а не от клиентов.** Когда LLM недоступна,
админу уходит алерт — с дедупом по типу проблемы, иначе на каждое сообщение
каждого клиента приходило бы по уведомлению, и админ отключил бы их ровно
тогда, когда они нужнее всего.

**Срок хранения персональных данных.** История и заявки удаляются по истечении
`RETENTION_DAYS_*` фоновой задачей, `/forget` удаляет данные по требованию
пользователя. Значение `0` отключает удаление — это должно быть осознанным
решением, а не значением по умолчанию.

---

## Деплой

Перед каждым выкатом: `python -m app.preflight`. Код возврата 1 — не выкатывать.

### Вариант 1. VPS + systemd (проще всего)

```bash
sudo useradd -r -m -d /opt/leadbot leadbot
sudo -u leadbot git clone <repo> /opt/leadbot
cd /opt/leadbot
sudo -u leadbot python3.11 -m venv .venv
sudo -u leadbot .venv/bin/pip install -r requirements.txt
sudo -u leadbot cp .env.example .env && sudo -u leadbot nano .env

sudo -u leadbot .venv/bin/python -m app.preflight   # обязательно
sudo cp deploy/leadbot.service /etc/systemd/system/
sudo systemctl daemon-reload && sudo systemctl enable --now leadbot
journalctl -u leadbot -f
```

### Вариант 2. Docker Compose

```bash
cp .env.example .env && nano .env
docker compose run --rm bot python -m app.preflight   # обязательно
docker compose up -d --build
docker compose logs -f bot
```

Контейнер работает от непривилегированного пользователя `leadbot` (uid 10001).

База лежит в именованном томе `bot-data`, `content/` смонтирован только на
чтение — прайс и FAQ правятся без пересборки образа.

### Вариант 3. PaaS (Railway, Fly, Render)

Переменные из `.env.example` задаются в панели платформы. Обязательно:

```dotenv
USE_WEBHOOK=True
WEBHOOK_BASE_URL=https://<ваш-домен>
PORT=8080
DB_PATH=/data/bot.sqlite3   # том, переживающий передеплой
```

⚠️ Без постоянного тома SQLite стирается при каждом деплое вместе с лидами.
На платформах с эфемерной файловой системой либо подключите том, либо
включите `LEAD_WEBHOOK_URL`, чтобы заявки уезжали во внешнюю систему.

### Вебхук вместо polling

```dotenv
USE_WEBHOOK=True
WEBHOOK_BASE_URL=https://bot.example.com   # только https
WEBHOOK_PATH=/telegram/webhook
WEBHOOK_SECRET=<длинная случайная строка>  # пусто — выведется из BOT_TOKEN
```

Приложение само вызывает `setWebhook` на старте и снимает его при остановке.
TLS терминируйте перед приложением (nginx / Caddy / балансировщик платформы).
Локально для проверки хватит туннеля: `ngrok http 8080` → полученный https
в `WEBHOOK_BASE_URL`.

### Бэкап базы

```bash
# в crontab пользователя leadbot
0 3 * * * /opt/leadbot/deploy/backup.sh
```

Скрипт снимает согласованный снимок на работающем боте (`Connection.backup()`,
а не `cp` — при включённом WAL копия могла бы попасть на середину транзакции),
жмёт gzip и удаляет архивы старше `KEEP_DAYS` (по умолчанию 30).

---

## Выгрузка лидов во внешнюю систему

Одна настройка вместо интеграции с конкретной CRM:

```dotenv
LEAD_WEBHOOK_URL=https://hook.eu2.make.com/xxxxxxxx
LEAD_WEBHOOK_SECRET=<случайная строка>
```

На каждую новую заявку уходит `POST` с JSON:

```json
{
  "event": "lead.created",
  "company": "Batumi Rent",
  "lead": {
    "id": 7,
    "created_at": "2026-09-03T12:00:00+00:00",
    "client_name": "Иван",
    "phone_or_contact": "+7 999 123-45-67",
    "contact_key": "tel:9991234567",
    "dates_or_timing": "12–19 августа",
    "service_details": "Toyota RAV4, нужен автомат",
    "budget": "до 100 GEL в сутки",
    "summary": "Клиент ищет кроссовер на неделю в августе.",
    "telegram_chat_id": 555,
    "telegram_user_id": 555,
    "telegram_username": "ivan"
  }
}
```

Приёмником может быть Make, Zapier, n8n, Google Apps Script или свой бэкенд —
а уже оттуда лид уезжает в Google Sheets, amoCRM, Bitrix24 или куда нужно.
Смена CRM у клиента становится изменением одной настройки, а не правкой кода.

Если задан `LEAD_WEBHOOK_SECRET`, в заголовке `X-Lead-Signature` приходит
`sha256=<HMAC-SHA256 от тела запроса>` — приёмник может убедиться, что запрос
от вас. Недоставленные заявки не теряются: они остаются в очереди и уходят
при следующем старте бота.

Проверить связь: `python -m app.preflight` дёргает приёмник тестовым запросом.

---

## Команды бота

| Команда   | Кто может | Действие                                          |
|-----------|-----------|---------------------------------------------------|
| `/start`  | все       | приветствие, сброс контекста                      |
| `/reset`  | все       | очистить историю диалога                          |
| `/forget` | все       | удалить все свои данные (история и заявки)        |
| `/help`   | все       | подсказка                                         |
| `/stats`  | админы    | воронка: пользователи, диалоги, лиды, конверсия   |
| `/export` | админы    | выгрузка всех заявок в CSV                        |

---

## Структура проекта

```text
app/
├── config.py                     # Pydantic Settings, разворачивает PROFILE
├── profiles.py                   # CLI по нишам: list / show / prompt
├── main.py                       # FastAPI, lifespan, webhook/polling, retention
├── preflight.py                  # «можно ли запускать?» — проверка перед деплоем
├── logging_config.py
├── bot/
│   ├── factory.py                # сборка Bot и Dispatcher
│   ├── keyboards.py              # кнопка «Отправить мой номер»
│   ├── handlers/
│   │   ├── commands.py           # /start, /reset, /forget, /stats, /export
│   │   ├── dialog.py             # текст и контакт → LLM
│   │   └── errors.py             # клиент не остаётся без ответа при сбое
│   ├── middlewares/
│   │   ├── throttling.py         # лимит частоты = лимит расходов
│   │   ├── logging.py
│   │   └── chat_guard.py         # только личные чаты: группа = слив бюджета
│   └── services/
│       ├── aggregator.py         # склейка очереди сообщений в один ход
│       ├── conversation.py       # оркестратор: история → LLM → tools → ответ
│       ├── lead_webhook.py       # выгрузка лида во внешнюю систему
│       └── notifier.py           # доставка лида админам
├── core/
│   ├── llm_client.py             # OpenAI-совместимый клиент + ретраи
│   ├── profile.py                # загрузка profiles/<имя>.toml
│   ├── prompts.py                # системный промпт (шаблон)
│   ├── tools.py                  # схемы save_qualified_lead и request_phone_button
│   └── schemas.py                # валидация аргументов инструмента
├── db/
│   ├── database.py               # SQLite (WAL) + миграции колонок
│   ├── models.py                 # схема и датаклассы
│   └── crud.py                   # история и лиды
└── utils/
    └── contacts.py               # нормализация контакта = ключ дедупа

profiles/*.toml                   # ниши: кто вы и что спрашивать у клиента
content/niches/*.md               # прайс и FAQ по нишам
content/knowledge.md              # база знаний, когда PROFILE не задан
docs/LAUNCH.md                    # чек-лист запуска демо
docs/NICHES.md                    # как завести свою нишу
deploy/leadbot.service            # systemd-юнит для VPS
deploy/backup.sh                  # согласованный бэкап SQLite
tests/                            # 218 тестов
```

---

## Смена LLM-провайдера

Меняются две переменные — код одинаков для всех OpenAI-совместимых API.

```dotenv
# OpenAI (дефолт)
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# OpenRouter — Claude 3.5 Haiku
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=anthropic/claude-3.5-haiku

# OpenRouter — что-нибудь подешевле
LLM_MODEL=google/gemini-flash-1.5
```

Требование к модели одно: поддержка tool calling. Без неё лиды не сохранятся.

---

## Тесты

```bash
pip install -r requirements-dev.txt
pytest -q
```

Покрыты: нормализация контактов, история с бюджетом символов, дедуп лидов,
tool-loop (успех, невалидные аргументы, битый JSON, неизвестный инструмент,
лимит раундов), кнопка запроса контакта, заявка из кнопки без участия LLM,
суточные лимиты, срок хранения и `/forget`, выход бота из групповых чатов,
дедуп алертов, склейка сообщений (включая потерю телефона из второго
сообщения), выгрузка лида вебхуком (подпись, ретраи, очередь), миграция
базы предыдущего релиза, деградация при недоступной LLM,
ответ клиенту при падении хэндлера, сериализация параллельных сообщений,
экранирование HTML в уведомлении, троттлинг, защита вебхука, `/stats`,
`/export`, профили ниш (комплектность поставляемых, приоритет `.env` над
профилем, влияние `qualify` на промпт) и сквозной путь «сообщение → лид →
уведомление».

Тесты отвязаны от личного `.env` (`_ignore_dotenv` в `tests/conftest.py`):
иначе поля подтягивались бы из него, и набор вёл бы себя по-разному локально
и в CI. CI прогоняет их на Python 3.11 и 3.12 (`.github/workflows/ci.yml`).

---

## Что осознанно не сделано в MVP

Честный список — чтобы вы решали, а не обнаруживали в бою.

- **SQLite, а не PostgreSQL.** До нескольких тысяч диалогов хватает с запасом.
  Горизонтальное масштабирование потребует смены на Postgres — слой `db/`
  изолирован ровно для этого.
- **Нет прямой интеграции с CRM.** Лид уходит вебхуком (`LEAD_WEBHOOK_URL`),
  а до amoCRM / Bitrix24 / Sheets его довозит Make, Zapier или n8n. Это
  сознательный размен: настройка вместо кода и никакой привязки к вендору,
  ценой одного посредника в цепочке. Если нужен прямой вызов API конкретной
  CRM — точка расширения `LeadWebhookSender.send`.
- **Троттлинг и дедуп алертов — в памяти процесса.** При нескольких репликах
  считаются независимо. Общий счётчик — Redis.
- **Нет автотеста с настоящей LLM.** Все тесты идут на дублёре модели:
  прогон против живого API стоил бы денег и был бы нестабильным. Реальную
  модель проверяет `python -m app.preflight`.
- **Нет распознавания голосовых.** Голосовые получают вежливый отказ;
  подключается через Whisper в `handlers/dialog.py`.
- **Дедуп по последним 10 цифрам номера.** Достаточно в рамках одного чата,
  но не различает номера разных стран с одинаковым хвостом.
