# Версии закреплены точно, а не диапазонами.
#
# Хостинг-панель выполняет `pip install -U -r requirements.txt` при каждом
# запуске сервера. С диапазонами обычный рестарт способен привезти новый мажор
# зависимости и уронить бота без единой правки в коде — снаружи это выглядит
# как «ничего не трогал, а он лег». К моменту закрепления `openai>=1.50` уже
# разъехался до 3.8.0, то есть на два мажора вперёд от того, под что писался
# код. Обновление версии должно быть отдельным коммитом с прогоном тестов,
# а не побочным эффектом перезапуска.
#
# Как обновлять: снять ограничение у нужного пакета, поставить, прогнать
# pytest, вписать полученную версию обратно.

aiogram==3.31.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.28.1
openai==3.8.0
aiosqlite==0.22.1
