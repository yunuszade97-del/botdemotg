-r requirements.txt

# Закреплены по той же причине, что и боевые зависимости: CI должен падать
# от изменений в коде, а не от вышедшего накануне pytest. `pytest>=8.0` к
# моменту закрепления уже разъехался до 9.1.1.
pytest==9.1.1
pytest-asyncio==1.4.0
